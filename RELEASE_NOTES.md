# Release Notes

## v0.1.1 - 2026-05-17

- 支持了本地任务图 UI，而不是以前只能查看静态 graph。
- 新增 `./start_ui.sh` 默认启动入口，打开 `http://127.0.0.1:8765/` 使用。
- UI 支持拖拽任务块、右键修改状态、右键编辑已有任务信息、在图上新建任务和 `Ctrl-Z` 撤销最近一次 UI 写入。
- CLI 创建和维护任务的方式继续保留。

## Release Rule

- 以后每次发版本都必须更新本文件，记录该版本的 release note。
